#version 460

in vec4 vaPosition;

uniform mat4 modelViewMatrix;
uniform mat4 projectionMatrix;

out vec3 viewDir;

void main() {
    vec4 pos = modelViewMatrix * vaPosition;
    viewDir = normalize(pos.xyz);
    gl_Position = projectionMatrix * pos;
}